name: Jekyll site CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:
    name: Build and deploy Jekyll (v4) for GitHub Pages
    # You may pin to the exact commit or the version.
    # uses: dunkmann00/jekyll-v4-gh-pages@b999b8b82ea52657255ff5db76f5183bf74d827a
    uses: dunkmann00/jekyll-v4-gh-pages@v1.0.0
